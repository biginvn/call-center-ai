;=== Voicebot Dialplan Configuration ===
; Thêm vào file extensions.conf trong context [internal]

; Voicebot extension - gọi từ browser hoặc internal
exten => 1000,1,NoOp(Incoming call to voicebot)
 same => n,Stasis(nixxis,"voicebot")
 same => n,Hangup()

; Voicebot extension với alias
exten => voicebot,1,NoOp(Incoming call to voicebot)
 same => n,Stasis(nixxis,"voicebot")
 same => n,Hangup()
